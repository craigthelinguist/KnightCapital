package storage;

/**
 * I'm just using this to load my tiles/other stuff until the real stuff comes along. You can safely
 * ignore this class and it will be deleted before the end.
 * @author craigthelinguist
 */
public class TemporaryLoader {

	// TemporaryLoader is a singleton - you cannot instantiate it, only use its static methods.
	private TemporaryLoader(){}
	
}
